import type { FC } from "react";
import { SloganReveal } from "@/app/_components/custom/slogan-reveal";

export const SloganWidget: FC = () => {
	return (
		<section>
			<SloganReveal />
		</section>
	);
};
